apiVersion: apps/v1
kind: Deployment
metadata:
  name: mfe-mdm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mfe-mdm
  template:
    metadata:
      labels:
        app: mfe-mdm
    spec:
      containers:
      - name: mfe-mdm
        image: ghcr.io/arda-labs/mfe-mdm:latest
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: mfe-mdm
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: mfe-mdm
