apiVersion: v1
kind: ConfigMap
metadata:
  name: mfe-shell-env-template
data:
  env.template.js: |
    (function(window) {
      window.__env = window.__env || {};
      window.__env.apiUrl = "${API_URL}";
    })(this);
