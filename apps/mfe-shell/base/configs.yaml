apiVersion: v1
kind: ConfigMap
metadata:
  name: mfe-shell-env-config
data:
  env.template.js: |
    (function(window) {
      window.__env = window.__env || {};
      window.__env.apiUrl = "${API_URL}";
      window.__env.authClientId = "${AUTH_CLIENT_ID}";
    })(this);
