apiVersion: apps/v1
kind: Deployment
metadata:
  name: seaweedfs-s3
  namespace: infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seaweedfs-s3
  template:
    metadata:
      labels:
        app: seaweedfs-s3
    spec:
      containers:
        - name: s3
          image: chrislusf/seaweedfs:latest
          args:
            - "s3"
            - "-filer=seaweedfs-filer.infra.svc.cluster.local:8888"
            - "-port=8333"
          ports:
            - containerPort: 8333
